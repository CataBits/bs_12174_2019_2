-- Banco de Dados da Aplicação "Sem Nome"
-- Apague este arquivo quando a modelagem estiver concluída!

-- Apagar o banco caso exista
DROP DATABASE IF EXISTS semnome;  
 
-- Criar o banco de Dados em UTF-8 e case-insensitive
CREATE DATABASE semnome CHARACTER SET utf8 COLLATE utf8_general_ci;  

-- Selecione o banco de dados
USE semnome;

-- Cria tabela "contatos"
CREATE TABLE contatos (
    id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    data TIMESTAMP DEFAULT CURRENT_TIMESTAMP, -- "AAAA-MM-DD hh:mm:ss"
    nome VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    assunto VARCHAR(255) NOT NULL,
    mensagem TEXT,
    campo1 TEXT COMMENT 'Campo adicional',
    campo2 TEXT COMMENT 'Campo adicional',
    status ENUM('recebido', 'lido', 'respondido', 'apagado') DEFAULT 'recebido'
);

-- Criar tabela "artigos"
CREATE TABLE artigos (
    id_artigos INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
    data TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    autor_nome VARCHAR(255),
    thumbnail VARCHAR(255) COMMENT 'Imagem que representa o artigo',
    titulo VARCHAR(255),
    resumo VARCHAR(255),
    conteudo LONGTEXT,
    visualizacoes INT,
    campo1 TEXT COMMENT 'Campo adicional',
    campo2 TEXT COMMENT 'Campo adicional',
    campo3 TEXT COMMENT 'Campo adicional',
    status ENUM('ativo', 'inativo') DEFAULT 'ativo'
);

-- Inserir dados de teste na tabela "contatos"
INSERT INTO contatos
    (nome, email, assunto, mensagem)
VALUES
    ('Joca da Silva', 'joca@silva.com', 'Contato do Joca', 'Olá! Como estão as coisas.')
;

INSERT INTO contatos
    (nome, email, assunto, mensagem)
VALUES
    ('Marineuza', 'mari@neuza.com', 'Contato da Marineuza', 'Olá! Como está tudo.'),
    ('Dilermano', 'diler@mano.com', 'Contato do Dilermano', 'Olá! Tá tudo certo?'),
    ('Setembrino', 'set@brino.com', 'Contato do Setembrino', 'Oba! Estamos felizes!!!')
;

-- Atividade:
-- Crie um insert de teste para a tabela artigos 
INSERT INTO artigos
    (autor_nome, thumbnail, titulo, resumo, conteudo, visualizacoes)
VALUES
    ('Joca', 'imagem.jpg', 'Teste de Artigo', 'Esse é apenas um teste.', 'Blá blá blá blá blá...', '0')
;

